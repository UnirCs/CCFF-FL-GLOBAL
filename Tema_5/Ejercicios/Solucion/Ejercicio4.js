// Declaramos la temperatura en grados Celsius
let celsius = 25; // Ejemplo: 25 grados Celsius

// Calculamos la temperatura en grados Fahrenheit
let fahrenheit = (celsius * 9/5) + 32;

// Mostramos el resultado en la consola
console.log(celsius + " grados Celsius equivalen a " + fahrenheit + " grados Fahrenheit");